<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>DevSkills — Junior Web Dev Quiz</title>
<link href="https://fonts.googleapis.com/css2?family=Nunito:wght@400;600;700;800;900&family=Nunito+Sans:wght@400;600&display=swap" rel="stylesheet">
<style>
*,*::before,*::after{box-sizing:border-box;margin:0;padding:0}
html{scroll-behavior:smooth}
:root{
  --bg:#f0edff;
  --card:#ffffff;
  --purple:#6c47ff;
  --purple-light:#ede9ff;
  --purple-mid:#a78bfa;
  --green:#22c55e;
  --green-light:#dcfce7;
  --red:#ef4444;
  --red-light:#fee2e2;
  --amber:#f59e0b;
  --amber-light:#fef3c7;
  --sky:#0ea5e9;
  --sky-light:#e0f2fe;
  --text:#1e1b4b;
  --text2:#6b7280;
  --text3:#9ca3af;
  --border:#e5e7eb;
  --shadow:0 2px 16px rgba(108,71,255,0.10);
  --shadow-lg:0 8px 32px rgba(108,71,255,0.16);
  --r:16px;--rsm:10px;
  --font:'Nunito',sans-serif;
  --body:'Nunito Sans',sans-serif;
}
body{background:var(--bg);font-family:var(--font);color:var(--text);min-height:100vh;overflow-x:hidden}
body::before,body::after{content:'';position:fixed;border-radius:50%;pointer-events:none;z-index:0;filter:blur(90px);opacity:0.40}
body::before{width:600px;height:600px;background:radial-gradient(circle,#c4b5fd,#818cf8);top:-150px;right:-120px}
body::after{width:450px;height:450px;background:radial-gradient(circle,#6ee7b7,#34d399);bottom:-100px;left:-100px}

.wrap{position:relative;z-index:1;max-width:680px;margin:0 auto;padding:2rem 1.5rem 6rem}

/* TOP BAR */
.top-bar{display:flex;align-items:center;justify-content:space-between;margin-bottom:1.75rem}
.logo{display:flex;align-items:center;gap:9px;font-size:18px;font-weight:900;color:var(--purple)}
.logo-icon{width:38px;height:38px;border-radius:11px;background:var(--purple);display:flex;align-items:center;justify-content:center}
.logo-icon svg{display:block}
.streak-badge{display:none;align-items:center;gap:6px;background:var(--card);border:2px solid #fde68a;border-radius:999px;padding:6px 14px;font-size:13px;font-weight:800;color:var(--amber);box-shadow:0 2px 12px rgba(245,158,11,0.15)}

/* MASTERY PROGRESS PANEL */
.progress-panel{display:none;background:var(--card);border-radius:var(--r);padding:1.25rem 1.5rem;box-shadow:var(--shadow);margin-bottom:1.5rem;gap:10px;flex-direction:column}
.pp-header{display:flex;justify-content:space-between;align-items:center}
.pp-title{font-size:12px;font-weight:800;color:var(--text2);text-transform:uppercase;letter-spacing:0.05em}
.pp-pct{font-size:15px;font-weight:900;color:var(--purple)}
.overall-bar{height:12px;background:#ede9ff;border-radius:99px;overflow:hidden}
.overall-bar-fill{height:100%;background:linear-gradient(90deg,var(--purple),var(--purple-mid));border-radius:99px;transition:width 0.6s cubic-bezier(.4,0,.2,1)}
.topic-pills{display:flex;flex-wrap:wrap;gap:6px}
.topic-pill{font-size:11px;font-weight:800;padding:5px 11px;border-radius:999px;border:2px solid transparent}
.pill-todo{background:#f3f4f6;color:var(--text3);border-color:#e5e7eb}
.pill-pass{background:var(--green-light);color:#16a34a;border-color:#bbf7d0}
.pill-fail{background:var(--red-light);color:#dc2626;border-color:#fecaca}
.pill-partial{background:var(--amber-light);color:#d97706;border-color:#fde68a}

/* SCREEN SYSTEM */
.screen{display:none}
.screen.active{display:block}

/* ── INTRO ── */
#screen-intro{animation:fadeUp .4s ease both}
.intro-hero{background:linear-gradient(135deg,#6c47ff 0%,#818cf8 100%);border-radius:var(--r);padding:2.5rem 2.5rem 3rem;color:#fff;margin-bottom:1.5rem;position:relative;overflow:hidden}
.intro-hero::before{content:'';position:absolute;right:-60px;bottom:-60px;width:250px;height:250px;border-radius:50%;background:rgba(255,255,255,0.07)}
.intro-hero::after{content:'';position:absolute;left:-30px;top:-30px;width:150px;height:150px;border-radius:50%;background:rgba(255,255,255,0.05)}
.intro-emoji{font-size:3.2rem;display:block;margin-bottom:14px;animation:bounce 2s infinite}
@keyframes bounce{0%,100%{transform:translateY(0)}50%{transform:translateY(-8px)}}
.intro-title{font-size:clamp(1.7rem,4.5vw,2.4rem);font-weight:900;line-height:1.15;margin-bottom:10px;position:relative}
.intro-sub{font-size:15px;opacity:.88;line-height:1.65;font-family:var(--body);position:relative}
.feature-grid{display:grid;grid-template-columns:1fr 1fr;gap:12px;margin-bottom:1.5rem}
.feature-card{background:var(--card);border-radius:var(--rsm);padding:1.1rem 1.25rem;box-shadow:var(--shadow);border:2px solid var(--border);transition:transform .15s,box-shadow .15s}
.feature-card:hover{transform:translateY(-3px);box-shadow:var(--shadow-lg)}
.feat-icon{font-size:1.6rem;margin-bottom:7px}
.feat-title{font-size:14px;font-weight:800;color:var(--text);margin-bottom:3px}
.feat-desc{font-size:12px;color:var(--text2);font-family:var(--body);line-height:1.5}
.btn-start{width:100%;padding:17px;font-size:16px;font-weight:900;font-family:var(--font);background:var(--purple);color:#fff;border:none;border-radius:var(--rsm);cursor:pointer;box-shadow:0 6px 20px rgba(108,71,255,.35);transition:all .2s;letter-spacing:.02em}
.btn-start:hover{background:#5b38e8;transform:translateY(-2px);box-shadow:0 10px 28px rgba(108,71,255,.4)}

/* ── QUIZ ── */
#screen-quiz{animation:fadeUp .3s ease both}
.round-label{display:inline-flex;align-items:center;gap:8px;background:var(--purple-light);color:var(--purple);font-size:13px;font-weight:800;padding:7px 15px;border-radius:999px;margin-bottom:1rem}
.round-dot{width:8px;height:8px;border-radius:50%;background:var(--purple);animation:pulse 1.5s infinite}
@keyframes pulse{0%,100%{opacity:1;transform:scale(1)}50%{opacity:.5;transform:scale(1.4)}}

.q-card{background:var(--card);border-radius:var(--r);box-shadow:var(--shadow-lg);padding:2rem;animation:cardIn .3s ease both}
@keyframes cardIn{from{opacity:0;transform:translateY(14px)}to{opacity:1;transform:translateY(0)}}

.q-card-head{display:flex;justify-content:space-between;align-items:center;margin-bottom:1.25rem}
.q-topic-chip{font-size:12px;font-weight:800;letter-spacing:.05em;text-transform:uppercase;background:var(--sky-light);color:var(--sky);padding:5px 13px;border-radius:999px}
.q-num{font-size:13px;font-weight:700;color:var(--text3)}
.card-dots{display:flex;gap:5px;margin-bottom:1.5rem}
.card-dot{flex:1;height:6px;border-radius:99px;background:#e5e7eb;transition:background .3s}
.card-dot.dc{background:var(--green)}
.card-dot.dw{background:var(--red)}
.card-dot.ds{background:var(--text3)}
.card-dot.act{background:var(--purple)}

.q-text{font-size:1.15rem;font-weight:700;line-height:1.6;color:var(--text);margin-bottom:1.75rem}

.options{display:flex;flex-direction:column;gap:10px;margin-bottom:1.75rem}
.opt{display:flex;align-items:center;gap:14px;background:#fafafa;border:2px solid var(--border);border-radius:var(--rsm);padding:13px 16px;cursor:pointer;text-align:left;transition:all .15s;font-family:var(--font);width:100%}
.opt:hover{border-color:var(--purple-mid);background:var(--purple-light);transform:translateX(4px)}
.opt:active{transform:translateX(2px) scale(.99)}
.opt.sel{border-color:var(--purple);background:var(--purple-light)}
.opt.idk{border-style:dashed;background:#fff}
.opt.idk:hover{border-color:var(--text3);background:#f9fafb}
.opt.idk.sel{border-color:var(--text3);background:#f3f4f6}
.opt.correct-ans{border-color:var(--green);background:var(--green-light);cursor:default;transform:none}
.opt.wrong-ans{border-color:var(--red);background:var(--red-light);cursor:default;transform:none}
.opt.dim{opacity:.4;cursor:default;transform:none!important}
.opt.dim:hover{border-color:var(--border);background:#fafafa}
.opt-key{width:28px;height:28px;border-radius:8px;background:#fff;border:2px solid var(--border);display:flex;align-items:center;justify-content:center;font-size:12px;font-weight:800;color:var(--text3);flex-shrink:0;transition:all .15s}
.opt.sel .opt-key{background:var(--purple);border-color:var(--purple);color:#fff}
.opt.correct-ans .opt-key{background:var(--green);border-color:var(--green);color:#fff}
.opt.wrong-ans .opt-key{background:var(--red);border-color:var(--red);color:#fff}
.opt.idk.sel .opt-key{background:var(--text3);border-color:var(--text3);color:#fff}
.opt-text{font-size:14px;font-weight:600;color:var(--text);line-height:1.4}
.opt.idk .opt-text{color:var(--text3);font-style:italic}

.feedback-box{border-radius:var(--rsm);padding:14px 16px;font-size:13px;font-family:var(--body);line-height:1.65;margin-bottom:1.5rem;display:none;animation:cardIn .25s ease}
.feedback-box.show{display:block}
.fb-ok{background:var(--green-light);border-left:4px solid var(--green);color:#166534}
.fb-no{background:var(--red-light);border-left:4px solid var(--red);color:#991b1b}
.fb-idk{background:var(--amber-light);border-left:4px solid var(--amber);color:#92400e}
.fb-head{font-weight:800;font-size:14px;margin-bottom:5px;display:flex;align-items:center;gap:6px}
.fb-yt{display:inline-flex;align-items:center;gap:6px;margin-top:10px;font-size:12px;font-weight:800;color:var(--purple);text-decoration:none;background:#fff;border:2px solid var(--purple-mid);padding:6px 13px;border-radius:999px;transition:all .15s}
.fb-yt:hover{background:var(--purple);color:#fff;border-color:var(--purple)}

.btn-row{display:flex;gap:10px;justify-content:flex-end}
.btn{font-family:var(--font);font-size:14px;font-weight:800;padding:12px 22px;border-radius:var(--rsm);cursor:pointer;transition:all .15s;border:none;letter-spacing:.01em}
.btn-ghost{background:transparent;color:var(--text3);border:2px solid var(--border)}
.btn-ghost:hover{border-color:var(--purple-mid);color:var(--purple)}
.btn-ghost:disabled{opacity:.3;cursor:not-allowed}
.btn-purple{background:var(--purple);color:#fff;box-shadow:0 4px 14px rgba(108,71,255,.3)}
.btn-purple:hover{background:#5b38e8;box-shadow:0 6px 20px rgba(108,71,255,.4);transform:translateY(-1px)}
.btn-purple:disabled{opacity:.35;cursor:not-allowed;transform:none;box-shadow:none}
.btn-green{background:var(--green);color:#fff;box-shadow:0 4px 14px rgba(34,197,94,.25)}
.btn-green:hover{background:#16a34a;transform:translateY(-1px)}

/* ── RESULTS ── */
#screen-results{animation:fadeUp .35s ease both}
.result-hero{border-radius:var(--r);padding:2.25rem;text-align:center;margin-bottom:1.5rem}
.hero-great{background:linear-gradient(135deg,var(--green),#4ade80)}
.hero-good{background:linear-gradient(135deg,#3b82f6,#60a5fa)}
.hero-ok{background:linear-gradient(135deg,var(--amber),#fcd34d)}
.hero-retry{background:linear-gradient(135deg,var(--purple),#a78bfa)}
.result-emoji{font-size:3.5rem;display:block;margin-bottom:8px}
.result-score{font-size:3.2rem;font-weight:900;color:#fff;line-height:1}
.result-sublabel{font-size:14px;color:rgba(255,255,255,.85);margin-top:5px;font-family:var(--body)}
.result-msg{font-size:1.1rem;font-weight:800;color:#fff;margin-top:10px}

.section-title{font-size:12px;font-weight:800;color:var(--text2);text-transform:uppercase;letter-spacing:.05em;margin-bottom:10px}
.mastery-grid{display:flex;flex-direction:column;gap:8px;margin-bottom:1.5rem}
.mastery-row{background:var(--card);border-radius:var(--rsm);padding:12px 16px;display:flex;align-items:center;gap:12px;box-shadow:var(--shadow);border:2px solid var(--border)}
.mastery-icon{font-size:1.2rem;flex-shrink:0}
.mastery-info{flex:1}
.mastery-name{font-size:13px;font-weight:800;color:var(--text);margin-bottom:1px}
.mastery-stat{font-size:11px;color:var(--text3)}
.mastery-bar-wrap{width:70px;flex-shrink:0}
.mastery-bar-track{height:6px;background:#e5e7eb;border-radius:99px;overflow:hidden}
.mastery-bar-fill{height:100%;border-radius:99px;transition:width .6s cubic-bezier(.4,0,.2,1)}
.mbar-pass{background:var(--green)}
.mbar-fail{background:var(--red)}
.mbar-partial{background:var(--amber)}
.mastery-badge{font-size:11px;font-weight:800;padding:4px 10px;border-radius:999px;flex-shrink:0}
.mb-pass{background:var(--green-light);color:#16a34a}
.mb-fail{background:var(--red-light);color:#dc2626}
.mb-partial{background:var(--amber-light);color:#d97706}
.mb-todo{background:#f3f4f6;color:var(--text3)}

.retry-banner{background:linear-gradient(135deg,var(--amber-light),#fef9c3);border:2px solid #fde68a;border-radius:var(--rsm);padding:1.25rem 1.5rem;margin-bottom:1.25rem}
.retry-title{font-size:15px;font-weight:800;color:#92400e;margin-bottom:5px;display:flex;align-items:center;gap:8px}
.retry-desc{font-size:13px;color:#b45309;font-family:var(--body);margin-bottom:12px;line-height:1.6}
.btn-retry-cta{width:100%;padding:14px;font-size:15px;font-weight:900;font-family:var(--font);background:var(--amber);color:#fff;border:none;border-radius:var(--rsm);cursor:pointer;box-shadow:0 4px 14px rgba(245,158,11,.3);transition:all .2s;letter-spacing:.01em}
.btn-retry-cta:hover{background:#d97706;transform:translateY(-1px);box-shadow:0 8px 22px rgba(245,158,11,.4)}
.btn-reset{width:100%;padding:14px;font-size:14px;font-weight:800;font-family:var(--font);background:transparent;color:var(--text3);border:2px solid var(--border);border-radius:var(--rsm);cursor:pointer;transition:all .2s;margin-top:8px}
.btn-reset:hover{border-color:var(--purple-mid);color:var(--purple)}

/* CONFETTI */
.confetti-wrap{position:fixed;inset:0;pointer-events:none;z-index:999;overflow:hidden}
.cp{position:absolute;top:-10px;border-radius:2px;animation:cfFall linear forwards}
@keyframes cfFall{to{transform:translateY(110vh) rotate(720deg);opacity:0}}

@keyframes fadeUp{from{opacity:0;transform:translateY(16px)}to{opacity:1;transform:translateY(0)}}

@media(max-width:520px){
  .feature-grid{grid-template-columns:1fr}
  .q-card{padding:1.5rem}
  .intro-hero{padding:2rem 1.75rem 2.5rem}
}
</style>
</head>
<body>
<div class="wrap">

  <div class="top-bar">
    <div class="logo">
      <div class="logo-icon">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="#fff" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
          <polyline points="16 18 22 12 16 6"/><polyline points="8 6 2 12 8 18"/>
        </svg>
      </div>
      DevSkills
    </div>
    <div class="streak-badge" id="streak-badge">
      🔥 <span id="streak-num">0</span> streak
    </div>
  </div>

  <!-- PROGRESS PANEL -->
  <div class="progress-panel" id="progress-panel">
    <div class="pp-header">
      <span class="pp-title">Your Mastery</span>
      <span class="pp-pct" id="overall-pct">0%</span>
    </div>
    <div class="overall-bar"><div class="overall-bar-fill" id="overall-fill" style="width:0%"></div></div>
    <div class="topic-pills" id="topic-pills"></div>
  </div>

  <!-- INTRO -->
  <div id="screen-intro" class="screen active">
    <div class="intro-hero">
      <span class="intro-emoji">🚀</span>
      <div class="intro-title">Junior Web Dev<br>Skill Check</div>
      <p class="intro-sub">10 questions across Terminal, Git &amp; macOS setup. Miss one? Study the explanation and retry — you keep going until you master every topic.</p>
    </div>
    <div class="feature-grid">
      <div class="feature-card">
        <div class="feat-icon">🎯</div>
        <div class="feat-title">10 Questions</div>
        <div class="feat-desc">Terminal, Git, SSH, Homebrew &amp; macOS setup essentials</div>
      </div>
      <div class="feature-card">
        <div class="feat-icon">🔄</div>
        <div class="feat-title">Learn &amp; Retry</div>
        <div class="feat-desc">Wrong answers come back — you only pass when you get it right</div>
      </div>
      <div class="feature-card">
        <div class="feat-icon">📈</div>
        <div class="feat-title">Track Progress</div>
        <div class="feat-desc">See your mastery build topic by topic in real time</div>
      </div>
      <div class="feature-card">
        <div class="feat-icon">🎓</div>
        <div class="feat-title">Guided Learning</div>
        <div class="feat-desc">Every miss links to a curated YouTube tutorial to fix the gap</div>
      </div>
    </div>
    <button class="btn-start" onclick="startQuiz()">Let's go! 🎉</button>
  </div>

  <!-- QUIZ -->
  <div id="screen-quiz" class="screen">
    <div class="round-label"><span class="round-dot"></span><span id="round-text">Round 1</span></div>
    <div class="q-card" id="q-card">
      <div class="q-card-head">
        <span class="q-topic-chip" id="q-topic">—</span>
        <span class="q-num" id="q-num">1 / 10</span>
      </div>
      <div class="card-dots" id="card-dots"></div>
      <p class="q-text" id="q-text"></p>
      <div class="options" id="options"></div>
      <div class="feedback-box" id="feedback"></div>
      <div class="btn-row">
        <button class="btn btn-ghost" id="btn-back" onclick="goBack()" style="display:none">← Back</button>
        <button class="btn btn-purple" id="btn-main" onclick="handleMain()" disabled>Check Answer</button>
      </div>
    </div>
  </div>

  <!-- RESULTS -->
  <div id="screen-results" class="screen">
    <div id="result-hero-el" class="result-hero hero-good">
      <span class="result-emoji" id="res-emoji">⭐</span>
      <div class="result-score" id="res-score">0/0</div>
      <div class="result-sublabel" id="res-label">correct this round</div>
      <div class="result-msg" id="res-msg">Nice work!</div>
    </div>
    <div class="section-title">Topic Mastery</div>
    <div class="mastery-grid" id="mastery-grid"></div>
    <div id="retry-section" style="display:none">
      <div class="retry-banner">
        <div class="retry-title">⚡ Almost there!</div>
        <p class="retry-desc" id="retry-desc">You missed <strong id="retry-count">0</strong> question(s). Study the explanations and resource links above, then smash the retry below!</p>
        <button class="btn-retry-cta" onclick="startRetry()">🔁 Retry missed questions</button>
      </div>
    </div>
    <div id="perfect-section" style="display:none">
      <div style="background:linear-gradient(135deg,#dcfce7,#bbf7d0);border:2px solid #86efac;border-radius:var(--rsm);padding:1.25rem 1.5rem;margin-bottom:1.25rem;text-align:center">
        <div style="font-size:2rem;margin-bottom:6px">🏆</div>
        <div style="font-size:15px;font-weight:800;color:#166534;margin-bottom:4px">100% Mastered!</div>
        <div style="font-size:13px;color:#16a34a;font-family:var(--body)">You've correctly answered every question. You're ready for the job!</div>
      </div>
    </div>
    <button class="btn-reset" onclick="resetAll()">↺ Start completely over</button>
  </div>

</div>
<div class="confetti-wrap" id="confetti-wrap"></div>

<script>
const ALL_Q=[
  {id:0,topic:"Terminal",icon:"💻",
   text:"You want to list all files in a directory, including hidden ones, with human-readable sizes. Which command is correct?",
   opts:["ls -la","ls -lh","ls -lAh","I don't know"],ans:2,
   exp:"<b>ls -lAh</b> combines long format (-l), all files excluding . and .. (-A), and human-readable sizes (-h). It's the most informative listing for spotting dotfiles.",
   link:"https://www.youtube.com/results?search_query=mac+terminal+ls+hidden+files+tutorial"},
  {id:1,topic:"Terminal",icon:"💻",
   text:"You accidentally started a long process and want to kill it immediately. What do you press?",
   opts:["Ctrl + Z","Ctrl + C","Ctrl + D","I don't know"],ans:1,
   exp:"<b>Ctrl + C</b> sends SIGINT to kill the running foreground process. Ctrl + Z suspends it (it's still alive in background). Ctrl + D closes the terminal session.",
   link:"https://www.youtube.com/results?search_query=mac+terminal+keyboard+shortcuts+beginner"},
  {id:2,topic:"Permissions",icon:"🔐",
   text:"Your deploy.sh script throws 'Permission denied'. What is the correct fix?",
   opts:["chmod +x deploy.sh","chown +x deploy.sh","sudo deploy.sh","I don't know"],ans:0,
   exp:"<b>chmod +x</b> adds the execute bit to the file. chown changes ownership, not permissions. sudo runs as root but doesn't grant the execute bit — it still needs to be set.",
   link:"https://www.youtube.com/results?search_query=chmod+file+permissions+mac+linux+tutorial"},
  {id:3,topic:"Homebrew",icon:"🍺",
   text:"You installed a Homebrew formula but the command isn't found in your shell. What should you try first?",
   opts:["Restart macOS","Run brew link <formula>","Run source ~/.zshrc or open a new terminal tab","I don't know"],ans:2,
   exp:"Your current shell session hasn't reloaded PATH. <b>Sourcing ~/.zshrc</b> (or opening a new tab) refreshes it and picks up Homebrew's bin folder. brew link is only needed for keg-only formulas.",
   link:"https://www.youtube.com/results?search_query=homebrew+mac+setup+PATH+zshrc+tutorial"},
  {id:4,topic:"SSH Keys",icon:"🔑",
   text:"You've generated a new SSH key pair. Which command loads the private key into the SSH agent?",
   opts:["ssh-add ~/.ssh/id_ed25519","ssh-keygen -t ed25519","cat ~/.ssh/id_ed25519.pub | pbcopy","I don't know"],ans:0,
   exp:"<b>ssh-add</b> registers the private key with the running SSH agent. ssh-keygen generates a key pair. The pbcopy command copies your public key to clipboard — great for GitHub, but doesn't load the key into the agent.",
   link:"https://www.youtube.com/results?search_query=mac+ssh+key+github+setup+tutorial"},
  {id:5,topic:"Git Branching",icon:"🌿",
   text:"Create a branch called feature/login AND switch to it in one single command.",
   opts:["git branch feature/login","git checkout feature/login","git checkout -b feature/login","I don't know"],ans:2,
   exp:"<b>git checkout -b</b> creates and switches in one step. git branch only creates. git checkout without -b switches to an existing branch and errors if it doesn't exist.",
   link:"https://www.youtube.com/results?search_query=git+branch+checkout+beginners+tutorial"},
  {id:6,topic:"Git Stash",icon:"📦",
   text:"You have uncommitted changes on feature/login but urgently need to switch to main to fix a bug. Best approach?",
   opts:["git reset --hard HEAD","git stash, then switch branches","git commit -m 'wip' then switch","I don't know"],ans:1,
   exp:"<b>git stash</b> shelves your changes so you can switch branches cleanly. git reset --hard permanently destroys uncommitted work. A WIP commit is valid but messier — stash is the cleaner tool here.",
   link:"https://www.youtube.com/results?search_query=git+stash+tutorial+explained+simply"},
  {id:7,topic:"Git Merging",icon:"🔀",
   text:"After merging feature/login into main, how do you delete the branch on the remote server?",
   opts:["git branch -d feature/login","git push origin --delete feature/login","git remote remove feature/login","I don't know"],ans:1,
   exp:"<b>git push origin --delete feature/login</b> removes the branch from the remote. git branch -d only deletes your local copy. git remote remove deletes your entire remote config — very destructive!",
   link:"https://www.youtube.com/results?search_query=git+delete+remote+branch+tutorial"},
  {id:8,topic:"Git Conflicts",icon:"⚡",
   text:"You resolved a merge conflict by editing the file. What are the correct next steps?",
   opts:["git merge --continue, then git push","git add <file>, then git commit","git stash, then git merge --abort","I don't know"],ans:1,
   exp:"After fixing conflict markers, <b>git add</b> tells Git the conflict is resolved, then <b>git commit</b> finalizes the merge. git merge --continue is for rebases, not standard merges.",
   link:"https://www.youtube.com/results?search_query=git+merge+conflict+resolution+tutorial"},
  {id:9,topic:"macOS Env",icon:"🍎",
   text:"In Finder on macOS, what keyboard shortcut toggles visibility of hidden dotfiles?",
   opts:["Cmd + Shift + H","Cmd + Shift + . (period)","Cmd + Option + H","I don't know"],ans:1,
   exp:"<b>Cmd + Shift + . (period)</b> is the native macOS shortcut to show/hide hidden files in Finder. Essential for accessing .env files, .ssh, .zshrc and other dotfiles.",
   link:"https://www.youtube.com/results?search_query=mac+show+hidden+files+finder+dotfiles"}
];

// state
let S={round:1,queue:[],cur:0,answered:false,selected:null,results:{},streak:0};

function startQuiz(){
  S={round:1,queue:ALL_Q.map((_,i)=>i),cur:0,answered:false,selected:null,results:{},streak:0};
  document.getElementById('progress-panel').style.display='flex';
  document.getElementById('streak-badge').style.display='flex';
  renderProgress();
  show('screen-quiz');
  renderQ();
}

function startRetry(){
  const missed=ALL_Q.filter(q=>S.results[q.id]&&!S.results[q.id].correct).map(q=>q.id);
  S.queue=missed;S.cur=0;S.round++;S.answered=false;S.selected=null;
  show('screen-quiz');renderProgress();renderQ();
}

function renderQ(){
  const qid=S.queue[S.cur];const q=ALL_Q[qid];const total=S.queue.length;
  document.getElementById('round-text').textContent=`Round ${S.round}`+(S.round>1?' — Retry ♻️':'');
  document.getElementById('q-topic').textContent=`${q.icon} ${q.topic}`;
  document.getElementById('q-num').textContent=`${S.cur+1} / ${total}`;
  document.getElementById('q-text').textContent=q.text;

  // dots
  const dc=document.getElementById('card-dots');dc.innerHTML='';
  S.queue.forEach((id,i)=>{
    const d=document.createElement('div');const r=S.results[id];
    d.className='card-dot'+(i===S.cur?' act':(r?(r.correct?' dc':' dw'):''));
    dc.appendChild(d);
  });

  // options
  const oc=document.getElementById('options');oc.innerHTML='';
  const L=['A','B','C','D'];
  q.opts.forEach((o,i)=>{
    const isIdk=i===3;
    const b=document.createElement('button');
    b.className='opt'+(isIdk?' idk':'');b.dataset.i=i;
    b.innerHTML=`<span class="opt-key">${L[i]}</span><span class="opt-text">${o}</span>`;
    b.onclick=()=>selectOpt(i);
    oc.appendChild(b);
  });

  // feedback reset
  const fb=document.getElementById('feedback');
  fb.className='feedback-box';fb.innerHTML='';

  // back btn
  const back=document.getElementById('btn-back');
  back.style.display=S.cur===0?'none':'';
  back.disabled=S.cur===0;

  // main btn
  const bm=document.getElementById('btn-main');
  bm.textContent='Check Answer';bm.className='btn btn-purple';bm.disabled=true;

  S.answered=false;S.selected=null;
  // re-trigger card animation
  const card=document.getElementById('q-card');
  card.style.animation='none';requestAnimationFrame(()=>{card.style.animation='';});
}

function selectOpt(i){
  if(S.answered)return;
  S.selected=i;
  document.querySelectorAll('.opt').forEach((b,bi)=>{b.classList.toggle('sel',bi===i);});
  document.getElementById('btn-main').disabled=false;
}

function handleMain(){
  if(!S.answered){checkAnswer();}else{nextQ();}
}

function checkAnswer(){
  const qid=S.queue[S.cur];const q=ALL_Q[qid];
  const sel=S.selected;const correct=sel===q.ans;const isIdk=sel===3;
  S.answered=true;

  // freeze opts
  document.querySelectorAll('.opt').forEach((b,i)=>{
    b.onclick=null;
    if(i===q.ans)b.classList.add('correct-ans');
    else if(i===sel&&!correct)b.classList.add('wrong-ans');
    else b.classList.add('dim');
    b.classList.remove('sel');
  });

  // record result
  const prev=S.results[qid]||{correct:false,attempts:0};
  S.results[qid]={correct,attempts:prev.attempts+1,topic:q.topic,icon:q.icon};

  // streak
  if(correct){S.streak++;}else{S.streak=0;}
  document.getElementById('streak-num').textContent=S.streak;

  // feedback
  const fb=document.getElementById('feedback');
  if(correct){
    fb.className='feedback-box fb-ok show';
    fb.innerHTML=`<div class="fb-head">🎉 Correct!</div><div>${q.exp}</div>`;
  }else if(isIdk){
    fb.className='feedback-box fb-idk show';
    fb.innerHTML=`<div class="fb-head">💡 Here's what you need to know</div><div><b>Answer: ${q.opts[q.ans]}</b><br><br>${q.exp}</div><a class="fb-yt" href="${q.link}" target="_blank" rel="noopener">▶ Watch tutorial</a>`;
  }else{
    fb.className='feedback-box fb-no show';
    fb.innerHTML=`<div class="fb-head">😅 Not quite — here's why</div><div><b>Correct: ${q.opts[q.ans]}</b><br><br>${q.exp}</div><a class="fb-yt" href="${q.link}" target="_blank" rel="noopener">▶ Watch tutorial</a>`;
  }

  // update btn
  const bm=document.getElementById('btn-main');
  const isLast=S.cur===S.queue.length-1;
  bm.textContent=isLast?'See Results →':'Next Question →';
  bm.className=correct?'btn btn-green':'btn btn-purple';
  bm.disabled=false;
  renderProgress();
}

function nextQ(){
  if(S.cur<S.queue.length-1){S.cur++;renderQ();}
  else{showResults();}
}
function goBack(){if(S.cur>0){S.cur--;renderQ();}}

function showResults(){
  show('screen-results');
  const total=S.queue.length;
  const correct=S.queue.filter(id=>S.results[id]&&S.results[id].correct).length;
  const pct=Math.round((correct/total)*100);

  let heroClass,emoji,msg;
  if(pct===100){heroClass='hero-great';emoji='🏆';msg='Flawless! Every question nailed.';}
  else if(pct>=70){heroClass='hero-good';emoji='⭐';msg='Really solid work!';}
  else if(pct>=40){heroClass='hero-ok';emoji='💪';msg='Good effort — let\'s close those gaps!';}
  else{heroClass='hero-retry';emoji='🌱';msg='Every attempt builds knowledge!';}

  const rh=document.getElementById('result-hero-el');
  rh.className=`result-hero ${heroClass}`;
  document.getElementById('res-emoji').textContent=emoji;
  document.getElementById('res-score').textContent=`${correct}/${total}`;
  document.getElementById('res-label').textContent=`correct this round`;
  document.getElementById('res-msg').textContent=msg;

  // mastery grid grouped by topic
  const topicMap={};
  ALL_Q.forEach(q=>{
    if(!topicMap[q.topic])topicMap[q.topic]={icon:q.icon,total:0,pass:0,attempted:false};
    topicMap[q.topic].total++;
    if(S.results[q.id]){topicMap[q.topic].attempted=true;if(S.results[q.id].correct)topicMap[q.topic].pass++;}
  });
  const mg=document.getElementById('mastery-grid');mg.innerHTML='';
  Object.entries(topicMap).forEach(([topic,d])=>{
    const done=d.pass===d.total&&d.attempted;
    const none=!d.attempted;
    const partial=d.attempted&&d.pass>0&&!done;
    const fail=d.attempted&&d.pass===0;
    const barPct=d.attempted?Math.round((d.pass/d.total)*100):0;
    const badgeCls=none?'mb-todo':(done?'mb-pass':(partial?'mb-partial':'mb-fail'));
    const badgeLabel=none?'Not reached':(done?'✓ Mastered':`${d.pass}/${d.total}`);
    const barCls=done?'mbar-pass':(partial?'mbar-partial':'mbar-fail');
    mg.innerHTML+=`<div class="mastery-row">
      <span class="mastery-icon">${d.icon}</span>
      <div class="mastery-info"><div class="mastery-name">${topic}</div><div class="mastery-stat">${d.total} question${d.total>1?'s':''}</div></div>
      <div class="mastery-bar-wrap"><div class="mastery-bar-track"><div class="mastery-bar-fill ${barCls}" style="width:${barPct}%"></div></div></div>
      <span class="mastery-badge ${badgeCls}">${badgeLabel}</span>
    </div>`;
  });

  // Retry or perfect
  const missed=ALL_Q.filter(q=>S.results[q.id]&&!S.results[q.id].correct);
  document.getElementById('retry-section').style.display=missed.length>0?'block':'none';
  document.getElementById('perfect-section').style.display=missed.length===0?'block':'none';
  if(missed.length>0){
    document.getElementById('retry-count').textContent=missed.length;
  } else {
    spawnConfetti();
  }
  renderProgress();
}

function renderProgress(){
  const total=ALL_Q.length;
  const passed=ALL_Q.filter(q=>S.results[q.id]&&S.results[q.id].correct).length;
  const pct=Math.round((passed/total)*100);
  document.getElementById('overall-pct').textContent=pct+'%';
  document.getElementById('overall-fill').style.width=pct+'%';

  const topicMap={};
  ALL_Q.forEach(q=>{
    if(!topicMap[q.topic])topicMap[q.topic]={total:0,pass:0,any:false};
    topicMap[q.topic].total++;
    if(S.results[q.id]){topicMap[q.topic].any=true;if(S.results[q.id].correct)topicMap[q.topic].pass++;}
  });
  const pp=document.getElementById('topic-pills');pp.innerHTML='';
  Object.entries(topicMap).forEach(([t,d])=>{
    const cls=!d.any?'pill-todo':(d.pass===d.total?'pill-pass':(d.pass>0?'pill-partial':'pill-fail'));
    pp.innerHTML+=`<span class="topic-pill ${cls}">${t}</span>`;
  });
}

function resetAll(){
  S={round:1,queue:[],cur:0,answered:false,selected:null,results:{},streak:0};
  document.getElementById('progress-panel').style.display='none';
  document.getElementById('streak-badge').style.display='none';
  show('screen-intro');
}

function show(id){
  document.querySelectorAll('.screen').forEach(s=>s.classList.remove('active'));
  document.getElementById(id).classList.add('active');
  window.scrollTo({top:0,behavior:'smooth'});
}

function spawnConfetti(){
  const w=document.getElementById('confetti-wrap');w.innerHTML='';
  const cols=['#6c47ff','#22c55e','#f59e0b','#ef4444','#0ea5e9','#a78bfa','#4ade80','#f472b6'];
  for(let i=0;i<90;i++){
    const p=document.createElement('div');p.className='cp';
    p.style.cssText=`left:${Math.random()*100}%;background:${cols[Math.floor(Math.random()*cols.length)]};width:${7+Math.random()*8}px;height:${7+Math.random()*8}px;border-radius:${Math.random()>.5?'50%':'2px'};animation-duration:${1.5+Math.random()*2.5}s;animation-delay:${Math.random()*.8}s;`;
    w.appendChild(p);
  }
  setTimeout(()=>{w.innerHTML='';},5000);
}
</script>
</body>
</html>
